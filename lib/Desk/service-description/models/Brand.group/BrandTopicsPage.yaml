extends: page
properties:
    entries:
        type: array
        items: { extends: TopicModel }
        location: embedded
    self: &SELF
        location: links
        data:
            operation: ListBrandTopics
            pattern: "#/topics\\??(?P<_query>.*)$#"
    first: *SELF
    last: *SELF
    next: *SELF
    previous: *SELF
